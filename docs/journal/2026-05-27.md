# 2026-05-27 · W3 D2

## 今天做了啥
- PrismaService + PrismaModule(`@Global()`)接入 AppModule
- `/api/health` 用 `SELECT 1` ping 数据库,返回 `{ status: 'ok', db: 'connected' }`
- 额外修了 3 个环境问题(细节见 `docs/troubleshooting.md`):
  - tsconfig rootDir:prisma.service.ts 从 `backend/prisma/` 移进 `backend/src/prisma/`
  - VS Code TS SDK 识别:加根 `tsconfig.json` + `.vscode/settings.json` 指 workspace TS
  - SQLite 路径:`DATABASE_URL` 从 `file:./prisma/dev.db` 改成 `file:./dev.db`

## 明天做啥
- W3 D3:全局响应拦截器,把 `/api/health` 的返回包成 W1 契约统一壳

## 有没有疑问
- 无