# 2026-05-28(周四)

## 今天做了啥
W3 D3 全局响应拦截器。写了 `response.interceptor.ts`,用 rxjs 的 map 把所有 controller 返回值包成 `{ code, data, message, traceId }` 统一壳,traceId 用 Node 原生 randomUUID 生成。在 main.ts 用 useGlobalInterceptors 全局注册,访问 /api/health 验证响应已经是 W1 契约的壳。在 api-contract.md 末尾加了一行指向拦截器文件位置。

## 明天做啥
W3 D4 全局异常过滤器,捕获 HttpException / Prisma 错误 / 未知 Error,映射到 W1 业务码,验证完删掉临时 throw。

## 有没有疑问
traceId 现在是每个响应临时生成的,真正的链路追踪要等它从请求入口透传下来——记一笔,本期不做。