# 2026-06-01 · W3 D5 · Swagger + 请求日志

## 今天做了啥
- 装 @nestjs/swagger / swagger-ui-express / nestjs-pino / pino-http / pino-pretty
- main.ts 配 Swagger,`/api/docs` 跑通,Swagger UI 能开,health 分组 + 中文 summary 都在
- app.module 接 pino logger(dev 走 pino-pretty singleLine,prod 走 JSON),Nest 日志全部走 pino
- /api/health 验证 200,每个请求一行 pino log

## 待修复
- path-to-regexp WARN:Express 5 下 Swagger 注册的 `/api/*` 通配路由被 auto-convert,导致最初 `/api/docs` 404
- 已用走法 A 绕过:`SwaggerModule.setup('api/docs', app, document, { useGlobalPrefix: false })`,功能正常
- 底层 Express 5 的 warning 可能仍在终端打印,纯噪音,不影响功能
- 根治留到 v2:对齐 express / path-to-regexp 版本(或锁 express@4),进 parking lot,今天不动

## 明天做啥
- W4 展开,Page Config 模块(list/detail/create/update/delete + 分页 + DTO)

## 疑问
- 无。W3 骨架周完成。